describe.skip("Notes", () => {
    
    describe("Given that the User is logged in", async () => {
        describe("Create a Note", () => {
            describe("Given that all the input data is valid", () => {
                test("It should return crea", () => {
                    expect(1+1).toBe(2)
                })
            })

            describe("Given that all the input data is invalid", () => {

            })

        })

        describe("Get Notes", () => {

        })

        describe("Update the Data of a Note", () => {

        })

        describe("Delete a Note", () => {

        })
    })

    describe("Given that the User is not logged in", async () => {
        
    })
})